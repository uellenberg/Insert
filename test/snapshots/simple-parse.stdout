// Constants: 

const val1: u32 = 10;
const val2: u32 = (5 + val1);

// Statics: 

static test: u32 = (val2 + (5 + ((2 * (8 - 5)) - 5)));

// Functions: 

function main() : () {
    let v: u32;
    v = 5;
    let g: u32;
    g = (5 * v);
    let h: bool;
    h = (v != g);
}