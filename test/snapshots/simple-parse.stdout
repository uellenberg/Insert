const val1: u32 = 10;
const val2: u32 = (5 + val1);
static test: u32 = (val2 + (5 + ((2 * (8 - 5)) - 5)));
static test2: bool = (val1 == val2);
function main() : () {
    let v: u32;
    v = 5;
    let g: u32;
    g = (5 * v);
    let h: bool;
    h = (v != g);
    test = v;
    test = g;
    test2 = h;
}