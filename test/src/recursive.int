//@check

function fib(v: u32) -> u32 {
    let test: string = "my test string\u0000";

    if v <= 1 {
        return v;
    }

    return fib(v - 1) + fib(v - 2);
}

function main() {
    fib(10);
}